# To Develop a Text Based Game.
#creating a Number Guessing Game.

#import a random function

import random
n = random.randrange(1,10) # Number shoulb be selected in the range of 1 to 10
guess = int(input("Enter any number: "))  # take the input from the user
while n!= guess:
    if guess < n:    
        print("Too low")
        guess = int(input("Enter number again: "))
    elif guess > n:
        print("Too high!")
        guess = int(input("Enter number again: "))
    else:
      break
print("you guessed it right!!")
